# Cookie/session settings
COOKIE_NAME=your_cookie_name
COOKIE_PASSWORD=long_secure_password_here

# MongoDB connection string (used when running with mongo store)
# Example for local MongoDB:
DB=mongodb://localhost:27017/sailPlacemarkCore

# Notes:
# - Start a local MongoDB server (e.g. `mongod`) before running tests that use the mongo store.
# - Alternatively, point DB to a cloud/remote MongoDB URI or use an in-memory MongoDB for CI.